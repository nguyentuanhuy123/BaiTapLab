class MathUtil{
    constructor(){}
    static add(a:number,b:number): number{
        return a+b;
    }
    static subtract(a:number,b:number): number{
        return a-b;
    }
    static multiple(a:number,b:number): number{
        return a*b;
    }
    static divine(a:number,b:number): number{
        return a/b;
    }
}